# After-Foundations → Worker-Matrix handoff — ancestry summary

> **Status**: `historical handoff summary`
> **Canonical source**: `docs/handoff/after-foundations-to-worker-matrix.md`
> **How to use this file**: preserve B8 carry-over constraints; let pre-worker-matrix mediate the actual kickoff

---

## 1. What from the B8 handoff is still directly useful

These B8 positions remain valid:

1. **`agent.core` is the host worker, not a binding slot**
2. the next phase should keep **service-binding / DO / platform law explicit**
3. worker naming and service names should stay coherent across docs and deploy config
4. open gates like `F03` / `F09` must remain visible rather than silently inherited away

---

## 2. What the B8 handoff no longer is

It is **not** the direct worker-matrix kickoff pack anymore.

That role has been replaced by:

1. `docs/issue/pre-worker-matrix/pre-worker-matrix-final-closure.md`
2. `docs/handoff/pre-worker-matrix-to-worker-matrix.md`
3. `docs/eval/worker-matrix/00-contexts/00-current-gate-truth.md`

So when B8 says “worker-matrix may start,” read that today as:

> after-foundations left a valid upstream handoff, and pre-worker-matrix later converted that ancestry into the actual rewrite-ready starting pack.

---

## 3. What r2 authors should keep from B8

Keep these ideas:

1. host-vs-remote split is a first-class design distinction
2. cross-worker platform law must stay evidence-backed
3. service names and binding names are related, but not the same thing
4. templates are starter shapes, not silent contract decisions
