# Worker Matrix — Root Index

> **Status**: `refreshed root index`
> **Purpose**: give `plan-worker-matrix.md` r2 a single top-level navigation surface for this folder
> **Last refreshed**: `2026-04-23`

---

## 0. One-line verdict

**`docs/eval/worker-matrix/` can now serve as the truth raw-material pack for worker-matrix r2: `00-contexts/` is the condensed gate layer, the 4 `*-core/` directories are re-certified worker detail packs, and the root-level docs are refreshed cross-worker derived views plus one explicit deferral note.**

---

## 1. What changed before this index was added

The folder is no longer in the state reflected by early `after-foundations` or pre-refresh worker evaluation notes.

Current baseline:

1. **pre-worker-matrix W0-W5 is closed**
2. **all 4 worker shells physically exist under `workers/`**
3. **`00-contexts/` has been rebuilt as the condensed truth layer**
4. **`agent-core/`, `bash-core/`, `context-core/`, and `filesystem-core/` have all been refreshed against current code truth**
5. the remaining root docs have been rewritten from old patch-note posture into current derived references

So the folder now has three distinct layers:

| layer | role | primary files |
|---|---|---|
| condensed gate truth | immediate kickoff / reading law | `00-contexts/00-current-gate-truth.md` |
| detailed worker truth | per-worker evidence and boundaries | `agent-core/`, `bash-core/`, `context-core/`, `filesystem-core/` |
| derived cross-worker views | interaction/readiness/deferral summaries | root-level `*.md` files in this directory |

---

## 2. Recommended reading order for r2 rewrite

1. `00-contexts/00-current-gate-truth.md`
2. `docs/issue/pre-worker-matrix/pre-worker-matrix-final-closure.md`
3. `docs/handoff/pre-worker-matrix-to-worker-matrix.md`
4. `00-contexts/03-evaluations/current-worker-reality.md`
5. `agent-core/index.md`
6. `bash-core/index.md`
7. `context-core/index.md`
8. `filesystem-core/index.md`
9. `cross-worker-interaction-matrix.md`
10. `worker-readiness-stratification.md`
11. `skill-core-deferral-rationale.md`
12. `context-space-examined-by-opus.md`

That order is intentional:

- **first** read direct gate truth,
- **then** read current worker-specific truth,
- **then** read the cross-worker derived summaries,
- **last** read the historical independent review.

---

## 3. What each root-level file now means

| file | role now | how to use it |
|---|---|---|
| `index.md` | top-level navigator | read first when entering the folder from scratch |
| `cross-worker-interaction-matrix.md` | cross-worker dependency and seam map | use when writing dependency graph / first absorption order |
| `worker-readiness-stratification.md` | global readiness grading | use when deciding Phase 0 vs later-wave work |
| `skill-core-deferral-rationale.md` | explicit scope guard for reserved `skill.core` | use to prevent fifth-worker scope creep |
| `context-space-examined-by-opus.md` | recalibrated historical independent review | use as ancestry / method audit, not as direct gate |

---

## 4. Reading law for the entire folder

When files disagree, use this precedence:

1. **pre-worker final closure / handoff**
2. **current code truth**
3. **refreshed `00-contexts/` condensed truth**
4. **refreshed 4 worker detail packs**
5. **refreshed root-level derived docs**
6. **older historical reviews / ancestry notes**

In practical terms:

1. do **not** reopen whether the 4 worker shells exist
2. do **not** treat remote protocol directions as already shipped runtime paths
3. do **not** demote the 4 worker packs back to speculative evaluation docs
4. do **not** let historical review notes outrank current code or refreshed gate truth

---

## 5. What r2 should inherit from this folder

`plan-worker-matrix.md` r2 should inherit the following posture:

1. worker-matrix is now an **assembly / absorption** problem, not a naming/bootstrap problem
2. `agent.core` remains the host/session edge owner
3. `bash.core` is the main first-wave remote execution seam
4. `context.core` and `filesystem.core` remain first-wave workers, but their most important current truths are still substrate truths, not already-live remote worker paths
5. `skill.core` stays reserved and explicitly deferred

---

## 6. Folder maintenance rule

If any of these change, this root layer should be refreshed in the same PR:

1. pre-worker / worker-matrix stage gate
2. `workers/*` shell topology or deploy status
3. W3 absorption map or blueprint baseline
4. any of the 4 worker `index.md` verdicts
5. `skill.core` scope posture

This directory is now meant to stay usable as **r2 rewrite raw material**, not drift back into an archaeological dump.
