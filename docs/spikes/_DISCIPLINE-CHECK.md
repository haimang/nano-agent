# Spike Discipline Check — Round 1 Bare-metal

> **Check date**: 2026-04-19
> **Spike scope**: `round-1-bare-metal/{spike-do-storage, spike-binding-pair}`
> **Reviewer**: Opus 4.7 (1M context)
> **Discipline source**: `docs/design/after-foundations/P0-spike-discipline-and-validation-matrix.md` §3
> **Required by**: `docs/action-plan/after-foundations/B1-spike-round-1-bare-metal.md` P6-01

---

## Verdict

✅ **PASSED — 7/7 disciplines satisfied**

All 7 spike disciplines have been observed throughout B1 Phase 1-5. No exceptions, no overrides.

---

## §1. 纪律 1：spike 代码放 `spikes/` 顶级目录，**不进** `packages/`

### Status: ✅ HOLDS

### Evidence

- `pnpm-workspace.yaml` only includes `packages/*`：
  ```yaml
  packages:
    - "packages/*"
  ```
- All spike code physically located under `spikes/round-1-bare-metal/`
- `find packages/ -path '*/spikes/*' -o -name 'spike*'` returns 0 matches
- 18 spike code files (Phase 1) + 14 spike code files (Phase 3) + scripts: **0 in packages/**

### Verification command

```bash
find packages/ -name 'spike*' -o -path '*/spikes/*' | wc -l   # → 0
find spikes/ -type f | wc -l                                  # → 45+
```

---

## §2. 纪律 2：spike 必须有 expiration date

### Status: ✅ HOLDS

### Evidence

`EXPIRATION_DATE=2026-08-01` 出现在所有 wrangler.jsonc 与 README：

| File | Line | Value |
|---|---|---|
| `spikes/README.md` | (top) | "Default expiration: 2026-08-01" |
| `spike-do-storage/README.md` | (top) | "Expiration: 2026-08-01" |
| `spike-do-storage/wrangler.jsonc` | `vars.EXPIRATION_DATE` | "2026-08-01" |
| `spike-binding-pair/README.md` | (top) | "Expiration: 2026-08-01" |
| `spike-binding-pair/worker-a/wrangler.jsonc` | `vars.EXPIRATION_DATE` | "2026-08-01" |
| `spike-binding-pair/worker-b/wrangler.jsonc` | `vars.EXPIRATION_DATE` | "2026-08-01" |

业主 B1 Q5 已批准: spike workers 可一直保留，由业主自主销毁；EXPIRATION_DATE 仍是 self-imposed cleanup signal。

---

## §3. 纪律 3：spike 不接 CI 主链

### Status: ✅ HOLDS

### Evidence

- `spikes/` 不在 `pnpm-workspace.yaml` 的 `packages` 数组中
- Repo root `package.json` 的 scripts 不引用 `spikes/`：
  - `test:contracts: node --test test/*.test.mjs`
  - `test:e2e: node --test test/e2e/*.test.mjs`
  - `test:cross: node --test test/*.test.mjs test/e2e/*.test.mjs`
- Spike 自己有独立 deploy script (`spikes/.../scripts/deploy*.sh`)，未挂任何 CI workflow
- Spike 失败不会阻塞主线 PR

---

## §4. 纪律 4：spike 的发现必须落到 design doc

### Status: ✅ HOLDS

### Evidence

13 required findings + 2 optional unexpected findings 全部落到 `docs/spikes/{namespace}/{NN}-{slug}.md`，使用 `docs/templates/_TEMPLATE-spike-finding.md` 8-section template：

```
docs/spikes/spike-do-storage/      9 finding docs (F01..F09)
docs/spikes/spike-binding-pair/    4 finding docs (F01..F04)
docs/spikes/unexpected/            2 optional finding docs (F01..F02)
docs/spikes/                       3 rollup docs (storage / binding / fake-bash-platform)
                                   1 _DISCIPLINE-CHECK.md (本文)
```

总计 **19 markdown files** under `docs/spikes/`. 所有 finding 都含 §3 Package Impact + §5 Writeback Action 必填字段。**纪律 4 是 Phase 4-5 的核心交付物**。

代码注释中的 spike-related context 均指向 finding doc，未有任何 platform truth 仅留在代码注释中。

---

## §5. 纪律 5：spike 不接生产数据 / 不持有业务数据 / 不实现新业务能力

### Status: ✅ HOLDS

### Evidence

| Aspect | Check |
|---|---|
| LLM API key | ❌ never wired — spike worker `vars` 中无 `LLM_API_KEY` 等 secret |
| 生产 R2 bucket | ❌ — spike 用 `nano-agent-spike-do-storage-probe` (spike-only namespace) |
| 生产 KV namespace | ❌ — spike 用 `nano-agent-spike-do-storage-kv` (spike-only) |
| 生产 D1 database | ❌ — spike 用 `nano_agent_spike_do_storage_d1` (spike-only, region APAC) |
| 业务数据 in spike payload | ❌ — 所有 probe 用 deterministic seed-generated bytes / placeholder strings |
| 新业务能力 | ❌ — spike 仅 healthz + probe routes + handlers; no business logic |
| 资源命名前缀 | ✅ all `nano-agent-spike-*` per owner Q1 dual-tag isolation |

---

## §6. 纪律 6：两轮 spike 分目录

### Status: ✅ HOLDS

### Evidence

```
spikes/
├── round-1-bare-metal/       ← B1 全部 spike 工作
│   ├── spike-do-storage/
│   └── spike-binding-pair/
└── round-2-integrated/       ← B7 工作（尚未启动；目录占位待 B7）
```

`round-1-bare-metal/` 包含所有 B1 ship 的 spike 代码；`round-2-integrated/` 在 B1 期间不存在文件（B7 启动时创建）。

无任何代码跨 `round-1-bare-metal/` 与 `round-2-integrated/` import。

---

## §7. 纪律 7：Round 1 spike 不依赖 packages/ 的运行时实现，但回写任务必须显式对齐 packages/ seam

### Status: ✅ HOLDS

### Evidence (Part A — 不依赖 packages/ 运行时)

```bash
grep -rn "from \"@nano-agent/" spikes/round-1-bare-metal/    # → 0 matches
grep -rn "import.*@nano-agent" spikes/round-1-bare-metal/    # → 0 matches
```

所有 spike worker 仅依赖 `@cloudflare/workers-types` (devDep)；**0 个 `import "@nano-agent/*"` 语句**。

### Evidence (Part B — 回写任务对齐 packages/ seam)

每条 finding 的 §3 Package Impact 都显式列出 packages/ 文件路径与行号（13 finding × 至少 1 个文件 = 至少 13 个 packages/ 引用）。代码注释中也显式 reference packages/ contract：

| Spike code file | References packages/ contract |
|---|---|
| `spike-do-storage/src/probes/bash-capability-parity.ts` | `packages/capability-runtime/src/capabilities/filesystem.ts:9,53,120-127` + `search.ts` |
| `spike-do-storage/src/do/ProbeDO.ts` | `MKDIR_PARTIAL_NOTE`, `RESERVED_PREFIX = "/_platform/"` (mirrors capability-runtime) |
| `spike-binding-pair/worker-a/src/worker.ts` | "fetch-based service binding seam" with explicit reference to `packages/session-do-runtime/src/remote-bindings.ts` |
| `spike-binding-pair/worker-b/src/handlers/hook-dispatch.ts` | mirrors `packages/hooks/src/runtimes/service-binding.ts` shape (without import) |
| `spike-binding-pair/worker-b/src/handlers/eval-emit.ts` | mirrors `packages/eval-observability/src/inspector.ts` event shape (without import) |

### Evidence (Part C — Writeback traceability is forward-confirmed)

3 个 rollup docs 的 §3 Writeback Destination Map 显式列出每个 finding 对应的 packages/ 文件路径，提供 forward traceability 给 B2-B6。

---

## Summary Matrix

| # | 纪律 | Status | Notes |
|---|---|---|---|
| 1 | spikes/ 顶级，不进 packages/ | ✅ | physical isolation; pnpm workspace excludes |
| 2 | EXPIRATION_DATE | ✅ | 2026-08-01 in 6 places (3 README + 3 wrangler.jsonc) |
| 3 | 不接 CI 主链 | ✅ | spikes/ not in workspace, not in CI scripts |
| 4 | finding → design doc | ✅ | 13+2 finding docs + 3 rollup docs + this discipline check |
| 5 | 不接生产数据 / 业务能力 | ✅ | spike-only namespaces; no LLM key; no business logic |
| 6 | round-1 与 round-2 分目录 | ✅ | round-1 fully populated; round-2 reserved for B7 |
| 7 | 不依赖 packages/ runtime；回写对齐 packages/ seam | ✅ | 0 `import "@nano-agent/*"` in spike code; 13 finding §3 sections cite packages/ files |

---

## Anti-pattern Audit (per design §4.4)

Confirmed **none** of the spike code violated the explicit anti-patterns listed in P0-spike-binding-pair-design / P0-spike-do-storage-design:

- ❌ "在 spike 里写未来产品也要用的代码" → no
- ❌ "在 spike 里实现新业务能力" → no
- ❌ "让 spike 通过 NACP 协议跟主链 worker 通讯" → no (spike 是孤岛)
- ❌ "给 spike 接生产 secret / 真实 LLM key" → no (确认无 secret)
- ❌ "把 spike 写成 5000 行的 god worker" → no (spike-do-storage worker.ts ~160 lines; ProbeDO ~260 lines; spike-binding-pair worker-a ~125 lines, worker-b ~103 lines)
- ❌ "让 spike 持有真实业务数据" → no (deterministic seed payloads only)
- ❌ "给 spike 加测试覆盖率要求" → no (spike is manual probe-driven)

---

## Approved for B1 closure

This discipline check authorizes B1 final closure issue and handoff to B2/B3/B4 ship phases.

**Next checkpoint**: When Round 2 integrated spike (B7) starts, run a fresh `_DISCIPLINE-CHECK-round-2.md` with the same 7 disciplines.

---

## References

- 7 disciplines source: `docs/design/after-foundations/P0-spike-discipline-and-validation-matrix.md` §3
- Action plan: `docs/action-plan/after-foundations/B1-spike-round-1-bare-metal.md` P6-01
- Charter: `docs/plan-after-foundations.md` §5.1
- Phase 5 closure: `docs/issue/after-foundations/B1-phase-5-closure.md`
